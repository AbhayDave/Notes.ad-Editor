export const getAllNotes = (state) => state.notes.allNotes;

export const getSelectedNoteId = (state) => state.notes.selectedNoteId;

export const getSelectedNoteForOption = (state) =>
  state.notes.selectedNoteForOption;
